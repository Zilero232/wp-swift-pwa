<?xml version="1.0"?>

<ruleset 
	name="WordPress"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd"
>
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>node_modules/*</exclude-pattern>

    <file>.</file>

    <config 
			name="installed_paths" 
			value="vendor/phpcompatibility/phpcompatibility-paragonie/,vendor/phpcompatibility/phpcompatibility-wp/,vendor/phpcompatibility/php-compatibility/,vendor/phpcsstandards/phpcsextra/,vendor/phpcsstandards/phpcsutils/,vendor/wp-coding-standards/wpcs/"
		/>

    <arg name="extensions" value="php"/>

    <arg name="colors"/>

    <arg name="report" value="full"/>

    <!-- Strip the filepaths down to the relevant bit. -->
    <arg name="basepath" value="."/>

    <!-- Check up to 8 files simultaneously. -->
    <arg name="parallel" value="8"/>

    <rule ref="WordPress-Extra">
        <exclude name="WordPress.PHP.YodaConditions"/>
        <exclude name="Universal.Arrays.DisallowShortArraySyntax.Found"/>
    </rule>

    <!-- Let's also check that everything is properly documented. -->
    <rule ref="WordPress-Docs"/>

    <!-- Check for PHP cross-version compatibility. -->
    <!-- Project should be compatible with PHP 8.* and higher. -->
    <config name="testVersion" value="8.0-"/>
    <rule ref="PHPCompatibilityWP">
        <include-pattern>*\.php</include-pattern>
    </rule>

    <config name="minimum_wp_version" value="6.8"/>

    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="swift-pwa"/>
            </property>
        </properties>
    </rule>
</ruleset>